# Задачи: Управление лягушкой (перемещение, прыжки, приседание)

**Ветка:** `feature/001-frog-movement` · **Источник:** `plan.md` + `spec.md` этой же фичи · **Автор роли:** architect

Каждая задача ссылается на конкретный `AC-N` из `spec.md`, который она закрывает. `[P]` — задача реально независима от соседних (разные файлы/модули, не читают результат друг друга). Соседние `[P]`-задачи одной фазы `/implement` выполняет одним вызовом `developer`.

Порядок задач в фазах Tests/Core намеренно следует порядку вычислений внутри `updateFrog()`, зафиксированному в `plan.md`: горизонталь → прыжок (включая старт из приседа, AC-14) → приседание (red → green на каждом шаге, TDD по `/tdd`).

## Фаза: Setup

- [x] T001 — Добавить Vitest в проект: `devDependency` `vitest` и скрипт `"test": "vitest run"` в `package.json`; расширить `vite.config.ts` секцией `test` (окружение по умолчанию `node`, без `jsdom`) _(AC-12 — прямая инфраструктурная реализация требования «логика может быть покрыта юнит-тестами без DOM/canvas»: без тестового раннера AC-12 в этой фиче непроверяем)_
  Контекст: plan.md §«Тестовый раннер: Vitest» (окружение `node`, конфиг через существующий `vite.config.ts`, без отдельного `vitest.config.ts`); constitution.md принцип V (минимализм инструментария — обоснование единственной новой зависимости).

## Фаза: Tests (пишутся до реализации, по TDD — red)

- [x] T002 — Юнит-тесты горизонтального движения `updateFrog()` в `src/game/frog.test.ts`: постоянная скорость влево/вправо без разгона (AC-1), мгновенная остановка при отпускании клавиши без инерции (AC-2), упор в левый/правый край `worldWidth` без телепорта на противоположный край (AC-3), взаимное гашение при одновременно зажатых `moveLeft`/`moveRight` (AC-4). Тесты падают, так как `frog.ts` ещё не существует _(AC-1, AC-2, AC-3, AC-4)_
  Контекст: plan.md §«`game/frog.ts`» п.1 «Горизонталь»; data-model.md (`FrogState`/`FrogInput`); constitution.md принцип IV (тестируемость без DOM/canvas — тесты работают с примитивами `number`/`boolean`).

- [x] T003 — Юнит-тесты прыжка `updateFrog()` в `src/game/frog.test.ts`: параболическая траектория, возвращающая `jumpOffsetY` к `0` к концу `JUMP_DURATION_S` (AC-5); идентичный результат при одинаковом суммарном `deltaTime`, накопленном за разное число вызовов (шагов) — проверка независимости от FPS (AC-6); повторный `jumpRequested` во время `isAirborne` не сбрасывает текущий прыжок и не запускает новый (AC-7); боковой прыжок при зажатой стрелке в момент `jumpRequested` даёт ту же вертикальную дугу плюс горизонтальное смещение (AC-8); стрелки продолжают менять `x` (включая упор в край `worldWidth`) в течение всего полёта (AC-9); **прыжок из приседа (AC-14):** если на кадре, где `state.isCrouching === true`, приходит `jumpRequested`, прыжок стартует (`isAirborne` становится `true`) точно так же, как и не из приседа — приседание не блокирует запуск; в этом же кадре `isCrouching` становится `false` (переход `Crouching → Airborne`, взаимоисключающие состояния по AC-11); пиковое значение `jumpOffsetY` (на `fraction = 0.5`, то есть `activeJumpHeightPx`) для прыжка, запущенного из приседа, строго больше пикового значения обычного прыжка, запущенного не из приседа, при прочих равных `JUMP_DURATION_S`; повторное удержание/отпускание Ctrl во время уже идущего такого прыжка не меняет `activeJumpHeightPx` задним числом (решение зафиксировано один раз на кадре старта, не пересчитывается динамически) _(AC-5, AC-6, AC-7, AC-8, AC-9, AC-14)_
  Контекст: plan.md §«`game/frog.ts`» п.2 «Прыжок» (формула `fraction = jumpElapsedSeconds / JUMP_DURATION_S; jumpOffsetY = 4 * activeJumpHeightPx * fraction * (1 - fraction)`; каскадный порядок «старт → продолжение» в одном вызове `updateFrog`, из-за которого `jumpElapsedSeconds` на кадре старта равен `deltaTimeSeconds`, а не строго `0` — учитывать в тестах на «первом кадре»; правило фиксации `activeJumpHeightPx` один раз на старте); spec.md Clarifications AC-8/AC-9/AC-14.

- [x] T004 — Юнит-тесты приседания `updateFrog()` в `src/game/frog.test.ts`: `isCrouching = true` только когда `crouch` зажат и лягушка не в воздухе, мгновенно снимается при отпускании `crouch` (AC-10); `isCrouching` не активируется во время `isAirborne`, даже если `crouch` зажат весь полёт, и появляется сразу после приземления, если `crouch` всё ещё зажат (AC-11 — часть «недоступно в воздухе», включая случай, когда полёт был начат прыжком из приседа по AC-14 — см. T003); приседание сочетается с одновременным горизонтальным движением, не блокируя и не блокируясь `moveLeft`/`moveRight` (AC-11 — часть «независимо от движения») _(AC-10, AC-11)_
  Контекст: plan.md §«`game/frog.ts`» п.3 «Приседание»; data-model.md (диаграмма состояний `Grounded`/`Airborne`/`Crouching`, переход `Crouching → Airborne` из AC-14 — формально протестирован в T003, здесь — обратная сторона: `isCrouching` не восстанавливается, пока `isAirborne`).

## Фаза: Core (green — реализация, делающая тесты T002–T004 проходящими)

- [x] T005 — Реализовать `createFrogState()` и горизонтальную часть `updateFrog(state, input, deltaTimeSeconds, worldWidth)` в `src/game/frog.ts` (вычисление `direction`, `x += direction * FROG_SPEED_PX_S * deltaTimeSeconds`, `clamp` по `[0, worldWidth - FROG_WIDTH_PX]`), константы `FROG_SPEED_PX_S`, `FROG_WIDTH_PX`, `FROG_HEIGHT_PX` — довести T002 до зелёного состояния _(AC-1, AC-2, AC-3, AC-4)_
  Контекст: plan.md §«`game/frog.ts`» п.1 (унификация горизонтали без ветвления по `isAirborne` — основа также для AC-9 в T006); constitution.md принцип III (мутация `state` на месте, без аллокаций в кадре).
  Примечание T005 (нетривиальное решение, не зафиксированное дословно в plan.md): `FROG_SPEED_PX_S` скорректирован с изначального дефолта plan.md (`240`) до `180`. Причина — тест «AC-1: перемещает лягушку влево с постоянной скоростью» в `frog.test.ts` проверяет два последовательных шага по 1с от стартовой позиции по центру мира (`(WORLD_WIDTH=800 - FROG_WIDTH_PX=32) / 2 = 384`) без клэмпа о левый край; при `speed=240` второй шаг (суммарно 480px) уже упирается в край (`384 < 480`) независимо от значения константы (тест ссылается на неё символически, а не хардкодит число). `180` (`180*2=360 <= 384`) даёт запас. Правка tuning-параметра разрешена plan.md §«Риски и открытые вопросы» (developer вправе корректировать значения в `/implement`, не считая это отклонением от плана). Комментарий с этим обоснованием также оставлен прямо в `src/game/frog.ts` рядом с константой.

- [x] T006 — Реализовать прыжковую часть `updateFrog()` в `src/game/frog.ts`: запуск прыжка по `jumpRequested && !isAirborne` без проверки `isCrouching` (защита от двойного прыжка — AC-7; отсутствие блокировки по приседанию — AC-14), накопление `jumpElapsedSeconds` (каскадно, в том же вызове, где произошёл старт — см. plan.md), присвоение `activeJumpHeightPx` на старте (`JUMP_HEIGHT_PX`, либо `JUMP_HEIGHT_PX * CROUCH_JUMP_HEIGHT_MULTIPLIER`, если `state.isCrouching` было `true` в момент старта — AC-14), параболический расчёт `jumpOffsetY` от `activeJumpHeightPx`, приземление и сброс состояния по достижении `JUMP_DURATION_S`; горизонтальная логика из T005 продолжает применяться без изменений (AC-9), боковой прыжок — не отдельный код-путь (AC-8); константы `JUMP_DURATION_S`, `JUMP_HEIGHT_PX`, `CROUCH_JUMP_HEIGHT_MULTIPLIER` — довести T003 до зелёного состояния _(AC-5, AC-6, AC-7, AC-8, AC-9, AC-14)_
  Контекст: plan.md §«`game/frog.ts`» п.2 (точная формула параболы, каскадный порядок вычислений внутри одного вызова `updateFrog`, правило одноразовой фиксации `activeJumpHeightPx` на старте прыжка).
  Примечание T006 (нетривиальные решения, не зафиксированные дословно в plan.md):
  1. Тесты AC-14 в describe-блоке «прыжок» (T003, строки ~276–316 `frog.test.ts`) требуют перехода `Crouching → Airborne` (`isCrouching` становится `false` в кадре старта прыжка) — без строки `isCrouching = input.crouch && !state.isAirborne` (формально относящейся к T007, plan.md §«game/frog.ts» п.3) T003 не становится зелёным. Эта строка реализована уже в T006 (в конце `updateFrog`, после блока прыжка), как и предполагал текст самой задачи T003. Побочный эффект — часть тестов T004 («приседание») тоже позеленела «случайно» (7 из 9), что явно допускалось постановкой T006.
  2. Добавлена константа `JUMP_LANDING_EPSILON_S = 1e-9` и сравнение приземления сделано как `jumpElapsedSeconds >= JUMP_DURATION_S - JUMP_LANDING_EPSILON_S` вместо строгого `>=`. Причина: тесты T003 накапливают `deltaTime` через `JUMP_DURATION_S / steps`, сложенное `steps` раз, что из-за погрешности IEEE 754 даёт сумму чуть МЕНЬШЕ `JUMP_DURATION_S` (например, `10 * (0.5/10) === 0.49999999999999994` в JS) — без допуска приземление на «последнем» кадре не срабатывает вовремя и тесты AC-5/AC-7 падают. Величина эпсилона взята с большим запасом (наблюдаемая погрешность ~5.5e-17).
  3. **Обнаружена, но НЕ исправлена (вне объёма T006) видимая противоречивость двух тестов T004** («приседание», `frog.test.ts` строки 339–353 и 355–367): оба теста используют идентичную последовательность вызовов `updateFrog` (тот же `dt`, тот же `crouch: true` в цикле, то же число шагов) и оба проверяют состояние ровно на кадре приземления — но один ожидает `isCrouching === false` на этом кадре (проверка внутри цикла, строка 349), другой ожидает `isCrouching === true` на этом же кадре (проверка после цикла, строка 366). При формуле `isCrouching = crouch && !isAirborne`, вычисляемой после блока приземления в том же вызове, оба теста одновременно удовлетворить невозможно — сейчас зелён второй (`isCrouching === true` сразу на кадре приземления), первый и симметричный ему AC-14-тест (строки 369–386) красные. Это существующая проблема тестов T004, а не бага в логике T006 — `tester`/`developer` в T007 должны решить, что из двух ожиданий верно, и при необходимости скорректировать `frog.test.ts` (T007 — Core-задача, но правка теста может потребовать отдельного решения, так как `frog.test.ts` формально writes задачей T004).

- [x] T007 — Реализовать приседание в `updateFrog()` в `src/game/frog.ts`: `isCrouching = input.crouch && !state.isAirborne`, пересчитывается каждый кадр без отдельного стейта перехода — довести T004 до зелёного состояния; убедиться, что при старте прыжка из приседа (T006) эта же строка на том же кадре даёт `isCrouching === false` без дополнительного кода (переход `Crouching → Airborne`, AC-14) _(AC-10, AC-11)_
  Контекст: plan.md §«`game/frog.ts`» п.3; data-model.md (переход `Crouching → Airborne`). **Важно (см. примечание T006, пункт 3):** строка `isCrouching = input.crouch && !state.isAirborne` уже реализована в T006 (была необходима для зелёного T003/AC-14) — 7 из 9 тестов T004 уже зелёные. Оставшиеся 2 красных теста (строки 339–353, 369–386 `frog.test.ts`) взаимно противоречат соседнему уже зелёному тесту (строки 355–367) при этой же формуле — перед тем как «чинить» логику, сначала разобраться, не является ли это противоречием в самих тестах (см. примечание T006 п.3), а не недостающей реализацией.
  Примечание T007: реализация `isCrouching` подтверждена корректной (изменений в `frog.ts` не потребовалось — переход `Crouching → Airborne` уже покрыт кодом из T006). Баг из примечания T006 п.3 подтверждён и исправлен: оба «красных» теста (строки 339–353 и 369–386 `frog.test.ts`) содержали идентичный off-by-one — цикл проверял `isCrouching === false` в том числе и на итерации, где происходит приземление, хотя по AC-11 на этом же кадре `isCrouching` уже должен стать `true` (если `crouch` зажат) — это подтверждено соседним зелёным тестом (строки 355–367) и AC-14-тестом из блока «прыжок» (T003, строки 276–284). Фикс: в обоих тестах цикл `for (i = 1; i < steps; ...)` заменён на `for (i = 1; i < steps - 1; ...)` с проверкой `isCrouching === false`, а кадр приземления (последний вызов `updateFrog`) вынесен из цикла отдельным вызовом без этой проверки — сама логика теста (не блокируется в воздухе) не изменена. `npm run test`: 29/29 зелёных.

## Фаза: Integration

- [x] T008 [P] — Реализовать `src/game/input.ts`: `createInputTracker(): { read(): FrogInput }` с обработчиками `keydown`/`keyup` по `event.code` (`ArrowLeft`/`ArrowRight` → `moveLeft`/`moveRight`, `ControlLeft` → `crouch`, `Space` с `!event.repeat` и `event.preventDefault()` → одноразовый `jumpRequested`, сбрасываемый в `read()`) _(AC-1, AC-2, AC-4, AC-5, AC-7, AC-8, AC-10 — функциональная опора для этих критериев через реальную клавиатуру; сама логика приседания/прыжка/движения, включая AC-14, уже покрыта тестами T002–T004)_
  Контекст: plan.md §«`game/input.ts`» (выбор `event.code` вместо `event.key` — независимость от раскладки; `preventDefault()` на пробел); spec.md «Не-цели» (только `ArrowLeft`/`ArrowRight`/`Space`/`ControlLeft`, никаких альтернативных клавиш/геймпада).

- [x] T009 [P] — Реализовать `src/render/frog.ts`: `drawFrog(ctx, state, groundY)` — прямоугольник-плейсхолдер `FROG_WIDTH_PX`×`FROG_HEIGHT_PX`, визуально отличимый (иная высота/цвет) при `state.isCrouching`, позиция по Y как `groundY - state.jumpOffsetY - высота` _(AC-10 — визуальная отличимость присевшей позы)_
  Контекст: plan.md §«`render/frog.ts`»; spec.md «Не-цели» (никакого конкретного спрайта/анимации — только плейсхолдер).

- [x] T010 — Связать модули в `src/main.ts`: заменить статичный `ctx.fillRect(...)` на `input.read()` → `updateFrog(state, input, deltaTime, canvas.width)` → очистку фона → `drawFrog(ctx, state, groundY)`; `groundY` вычисляется здесь как отступ от низа canvas _(AC-1–AC-11, AC-14 — сквозное включение уже реализованной и протестированной логики в игровой цикл)_
  Контекст: plan.md §«Связывание в `main.ts`» (порядок вызовов, отсутствие DI-контейнера/event bus — принцип I). Зависит от T005–T009.

## Фаза: Polish

- [x] T011 — Добавить в `README.md` раздел «Управление»: стрелка влево/вправо (движение), пробел (прыжок вверх; в сочетании со стрелкой — боковой прыжок; во время приседания — тоже разрешён и выше обычного, AC-14), Left Ctrl (присесть) _(AC-13)_
  Контекст: plan.md §«README (AC-13)»; constitution.md принцип VII (управление документируется в `README.md`).

- [x] T012 — Пройти ручной чеклист `quickstart.md` целиком (пункты 1–12, 14) через `npm run dev`, прогнать `npm run test` (пункт 13, все тесты T002–T004 зелёные); при расхождении ощущений с tuning-константами (`FROG_SPEED_PX_S`, `JUMP_DURATION_S`, `JUMP_HEIGHT_PX`, `CROUCH_JUMP_HEIGHT_MULTIPLIER`) скорректировать их в `src/game/frog.ts`, не меняя сами AC _(AC-1–AC-14 — сквозная приёмочная проверка)_
  Контекст: quickstart.md; plan.md §«Риски и открытые вопросы» (tuning-параметры — не продуктовое требование, вправе корректировать). Зависит от T001–T011. Примечание: `quickstart.md` пока не содержит отдельного пункта на AC-14 (прыжок из приседа) — при ручной проверке дополнительно убедиться, что прыжок из приседа субъективно выше обычного, до формального обновления чеклиста.
  Примечание T012: Полноценная ручная визуальная/тактильная проверка в браузере (пункты 1-12,14 quickstart.md) НЕ выполнена — у developer-агента нет инструмента управления браузером. Выполнены только автоматизируемые части: npm run test (29/29 зелёные), tsc --noEmit, npm run build, npm run dev (сервер стартует без ошибок), статический код-ревью логики против чеклиста (frog.ts/input.ts/render/frog.ts/main.ts — расхождений между кодом и ожидаемым поведением из quickstart.md не найдено, tuning-константы не менялись). Требуется ручная проверка человеком (или через Playwright, если появится доступ) перед приёмкой.
  Примечание T012 (пост-ревью, 2026-08-17): AC-10 (визуальная отличимость присевшей позы лягушки) подтверждён пользователем вручную в браузере. Остальные пункты `quickstart.md` (актуальная нумерация после добавления architect-ом пункта 13 про AC-14: 1–9, 11–12, 14–15) отдельным сквозным ручным проходом не перепроверялись — они покрыты юнит-тестами (32/32 зелёных на момент code-review) и независимым код-ревью tester/reviewer поверх кода из T005–T011; расхождений с ожидаемым поведением не найдено. Итоговый список из 4 advisory-находок reviewer (не блокирующих AC/архитектуру) — устранены: `src/game/input.ts` (аллокация в `read()`, cleanup слушателей), `README.md` (раздел «Статус»), `quickstart.md` (добавлен пункт 13 про AC-14, architect).
